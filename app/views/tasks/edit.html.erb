<h1>Edit Task</h1>
<%= form_for(@tasks) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <%= t.submit %>
<% end %>
<%= link_to "Back to main list", tasks_path %>
