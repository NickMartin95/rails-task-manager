<%= form_for(tasks) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :address %>
  <%= t.text_field :address %>
  <%= t.label :rating %>
  <%= t.number_field :rating %>
  <%= t.submit %>
<% end %>
<%= link_to "Back to main list", tasks_path %>
